# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/

# Donnees lourdes : snapshots CDragon pines (24+ MB) — pas dans le repo
/data/cdragon/*.json
# Snapshots datatft : COMMITES (48 K, pinnes pour une CI deterministe qui ne frappe
# pas l'API non-officielle depuis les runners ; regenerables via scrape_datatft)
# Parties collectees (regenerables via collect_matches)
/data/matches/

# Checkpoints / runs d'entrainement
/runs/

# Secrets
.env

# Claude Code runtime (keep skills, drop local/runtime state)
.claude/scheduled_tasks.lock
.claude/settings.local.json
.DS_Store
.gstack/
